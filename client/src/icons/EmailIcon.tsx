import { AiOutlineMail } from 'react-icons/ai'
import { IconProps } from './icon'

const EmailIcon = ({ className }: IconProps) => (
	<AiOutlineMail className={className} />
)

export default EmailIcon
